export interface Referente {
    id: string;
    email: string;
    nome?: string;
    cognome?: string;
    codice_fiscale?: string;
    sesso?: string;
    cittàNascita?: string;
    indirizzo?: string;
}